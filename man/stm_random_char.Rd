% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm_random_char.R
\name{stm_random_char}
\alias{stm_random_char}
\title{Generate a random string of characters}
\usage{
stm_random_char(
  n = 8,
  numbers = TRUE,
  include_lowercase = TRUE,
  include_uppercase = TRUE,
  set_seed = NULL
)
}
\arguments{
\item{n}{The length of the random string to generate. Defaults to 8.}

\item{numbers}{Whether to include numeric digits (0-9). Defaults to 
\code{TRUE}.}

\item{include_lowercase}{Whether to include lowercase letters. Defaults to 
\code{TRUE}.}

\item{include_uppercase}{Whether to include uppercase letters. Defaults to 
\code{TRUE}.}

\item{set_seed}{Optional. An integer seed for random number generation.
Defaults to \code{NULL} (no fixed seed).}
}
\value{
A random string of characters of length \code{n}.
}
\description{
\code{stm_random_char()} generates a random string of characters of 
specified length, including numbers, lowercase, and uppercase letters
based on user-defined options.
}
\examples{
# Generate a random string with default parameters
stm_random_char()

# Generate a random string with only lowercase letters
stm_random_char(include_uppercase = FALSE)

# Generate a random string with only numbers
stm_random_char(include_lowercase = FALSE, include_uppercase = FALSE)

}
\seealso{
\code{\link{stm_random_number}}, \code{\link{stm_random_vector}}, \code{\link{stm_random_matrix}}
}
