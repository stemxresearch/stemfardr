% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm_to_latex.R
\name{stm_to_latex}
\alias{stm_to_latex}
\title{Convert matrix or string to LaTeX format}
\usage{
stm_to_latex(
  x,
  align = c("right", "center", "left"),
  symbol = "$",
  name = NULL,
  digits = NULL
)
}
\arguments{
\item{x}{The matrix, vector, or string to convert to LaTeX format.}

\item{align}{Optional. The alignment for matrix columns. Choices are 
\code{"right", "center",} or \code{"left"}. Default is
\code{"right"}.}

\item{symbol}{Optional. The symbol to wrap around the LaTeX expression. 
Default is \code{"$"}.}

\item{name}{Optional. The name for the equation. If provided, it will 
appear before the LaTeX code.}

\item{digits}{Optional. The number of digits to round the matrix elements 
to. Default is \code{NULL} (no rounding).}
}
\value{
A LaTeX formatted string.
}
\description{
\code{stm_to_latex()} converts a matrix or string into LaTeX code for
rendering mathematical expressions. Supports optional alignment and 
decimal rounding.
}
\details{
The function supports both converting a matrix and rendering a string 
as a LaTeX equation. It also allows custom alignment of matrix columns 
and rounding of matrix values to the specified number of digits.
}
\examples{
stm_to_latex("x^2 + y^2 - z^2")
stm_to_latex(matrix(1:4, nrow = 2), align = "center", digits = 2)
stm_to_latex(matrix(1:4, nrow = 2), name = "A")

}
\seealso{
\code{\link[Ryacas]{tex}}
}
